# 5일차 복습

## 🖥️ 프로그램 처리 방식의 기본 순서

**입력 → 메모리 저장 → CPU 연산 → 메모리 저장 → 출력**

| 순서 | 단계 설명                  | 내용                                                |
| -- | ------------------         | ----------------------------------                 |
| ①  | **데이터 입력**              | 키보드, 마우스, 파일 등을 통해 프로그램에 데이터가 들어옴  |
| ②  | **메모리에 저장**            | 입력된 데이터를 **RAM(메모리)**에 임시로 저장            |
| ③  | **CPU 연산 처리**           | CPU가 메모리에 있는 데이터를 가져와 명령에 따라 계산·처리  |
| ④  | **처리 결과를 메모리에 저장** | CPU가 처리한 결과를 다시 메모리에 저장                  |
| ⑤  | **출력 장치로 전송**         | 처리된 결과를 모니터, 프린터 등의 출력 장치로 전달        |

* **RAM** → CPU가 처리할 데이터를 임시로 저장하는 공간
* **CPU** → 실제 계산과 명령어 처리를 담당
* **입력 장치** → 키보드, 마우스, 파일 등
* **출력 장치** → 모니터, 프린터 등

---

## 📚 자료구조

> **자료구조 = 데이터를 컴퓨터에 효율적으로 저장하고 관리하는 방법**

### 기본 개념

* **정보** → 어떤 것을 판단하거나 처리할 때 필요한 유용한 데이터
* 정보를 컴퓨터에서 사용하려면 **메모리(저장 공간)**가 필요함
* 데이터를 무작정 저장하는 것이 아니라, **효율적으로 저장·관리·사용하기 위해 자료구조**를 사용함

### 예시

* 여러 개의 데이터를 순서대로 저장 → **리스트**
* 데이터를 빠르게 찾기 → **딕셔너리**
* 먼저 들어온 데이터를 먼저 처리 → **큐**

### 개발에서의 큰 흐름

```text
자료구조 & 알고리즘 → 운영체제(OS) → 네트워크
```

* **자료구조** → 데이터를 어떻게 저장하고 관리할 것인가?
* **알고리즘** → 데이터를 어떻게 효율적으로 처리할 것인가?
* **운영체제(OS)** → 컴퓨터의 자원(CPU, 메모리 등)을 어떻게 관리할 것인가?
* **네트워크** → 컴퓨터끼리 데이터를 어떻게 주고받을 것인가?

---

## 🐍 VS Code 가상환경 자동 인식

VS Code에서 가상환경을 선택하려면:

1. `Ctrl + Shift + P` 입력
2. **Python: Select Interpreter** 선택
3. 내가 사용할 가상환경 선택

---

# 파이썬 기본 문법 시작하기

표현식, 식별자, 키워드, 연산자 등의 기본 구성 요소가 모여 **문장(실행할 수 있는 최소 단위)**을 만든다.

이후 문장들이 모여 **프로그램**이 된다.

---

## 1. 문장(Statement)

**문장(Statement)** : 실행할 수 있는 코드의 최소 단위. 문장들이 모이면 **프로그램**이 된다.

## 2. 프로그램

**프로그램** : 컴퓨터가 특정한 일을 수행하도록 명령어들(문장)을 모아 만든 것.

* **자료(Data)** → 프로그램이 처리할 수 있는 모든 것
* **처리(Process)** → 자료를 원하는 결과로 바꾸거나 사용하는 것

### 프로그래밍

**프로그래밍** → 프로그램을 만드는 과정 → 자료를 처리하는 명령어를 작성하는 것

---

## 3. 인터프리터 방식

**인터프리터 방식** : 프로그램의 명령어를 한 번에 하나씩 해석하고 실행하는 방식

---

## 4. 객체 지향 프로그래밍(OOP)

**객체 지향 프로그래밍(OOP)** : 프로그램을 **객체** 중심으로 구성하는 프로그래밍 방식

### 객체(Object)

**객체(Object)** : 현실 세계의 **보이는 것과 보이지 않는 모든 것**을 포함하며, 프로그램에서 표현하고 싶은 '대상'

현실 세계의 대상을 그대로 컴퓨터에 옮길 수는 없기 때문에,

**일반화 + 추상화**

과정을 거쳐 프로그램에서 표현한다.

→ 이렇게 옮겨진 결과가 **클래스(Class)**

### 클래스(Class)

**클래스** : 데이터(객체가 가지고 있는 정보) + 행위(객체가 수행할 수 있는 동작)

### 예시

카메라로 사진을 찍는다.

* 사진 → 자료
* 사진을 저장한다 → 처리

---

## 5. 표현식(Expression)

**표현식(Expression)** : 파이썬에서 어떠한 값을 만들어 내는 간단한 코드를 표현식이라 부른다.

값은 수식, 숫자, 문자열 등을 말한다.

---

## 6. 키워드(Keyword)

**키워드(Keyword)** : 특별한 의미가 부여된 단어로, 파이썬이 만들어질 때 이미 사용하겠다고 예약해둔 것들.

---

## 7. 식별자(Identifier)

**식별자(Identifier)** : 프로그래밍 언어에서 이름을 붙일 때 사용하는 단어.

주로 변수 또는 함수 이름 등으로 사용된다.

---

## 8. 주석(Comment)

**주석(Comment)** : 프로그램의 진행에 전혀 영향을 주지 않는 코드로, 프로그램을 설명하기 위해 사용.

---

## 9. 연산자

**연산자** : 스스로 값이 되는 것이 아닌 값과 값 사이에 무언가 기능을 적용할 때 사용하는 것.

예: `+`, `-`

---

# 자료와 자료형

## 자료(리터럴, Literal)

**자료(리터럴, Literal)** : 숫자든 문자든 어떠한 **값 자체**를 의미한다.

## 자료(Data)

**자료(Data)** : 프로그램이 처리할 수 있는 모든 것

예:

* 사진
* 메시지
* 경험치

## 자료형(Data Type)

**자료형(Data Type)** : 자료를 기능과 역할에 따라 구분한 종류

### 주요 자료형

* **문자열(String)** → 예: `"안녕하세요"`
* **숫자(Number)** → 예: `52`, `273`, `103.32`
* **불(Boolean)** → 예: `True`, `False`

### 자료형 확인

`type()` 함수를 사용하여 자료형을 확인할 수 있다.

```python
type("안녕하세요")
type(52)
type(True)
```

---

# 이스케이프 문자

| 이스케이프 문자 | 의미      |
| -------- | ------- |
| `\"`     | 큰따옴표    |
| `\'`     | 작은따옴표   |
| `\n`     | 줄바꿈     |
| `\t`     | 탭       |
| `\\`     | 역슬래시 자체 |

---

# 숫자 자료형

## 정수(Integer)

**정수(Integer)** : 소수점이 없는 숫자

예:

```text
0
1
273
-52
```

## 실수(Float)

**실수(부동 소수점, Floating Point)** : 소수점을 포함하는 숫자

예:

```text
0.0
52.273
-1.2
```

---

# 연산자

## 사칙 연산자

`+`, `-`, `*`, `/`

예:

```python
10 + 3
10 - 3
10 * 3
10 / 3
```

## 정수 나누기 연산자 `//`

숫자를 나누고 소수점 이하의 자릿수를 떼어 버린 후 정수 부분만 남기는 연산자입니다.

```python
10 // 3
# 3
```

## 나머지 연산자 `%`

A를 B로 나누었을 때 남은 나머지를 구하는 연산자입니다.

```python
10 % 3
# 1
```

## 제곱 연산자 `**`

숫자를 제곱하는 연산자입니다.

```python
2 ** 3
# 8
```

---

# TypeError 예외

**TypeError 예외** : 서로 다른 자료형을 연산하면 발생할 수 있는 오류입니다.

예:

```python
"10" + 10
```

문자열과 숫자를 서로 더하려고 하기 때문에 `TypeError`가 발생합니다.
